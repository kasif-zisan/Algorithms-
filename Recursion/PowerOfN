#include<bits/stdc++.h>
using namespace std;
int power(int n,int m){
    if(m==0) return 1;
    int sub = power(n,m-1);
    int result = n*sub;
    return result;
}
int main(){
    int n,m;
    cin>>n>>m;
    cout<<power(n,m)<<endl;
}
